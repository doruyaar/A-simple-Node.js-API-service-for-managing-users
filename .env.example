# copy this file to .env and fill the real values
# .env is gitignored so the secret never gets commited

# secret used to sign the JWT tokens
JWT_SECRET=

# port the server listen on (optional, default 3000)
PORT=3000
